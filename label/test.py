import getRecRelTag
import threading
file = "./data/relics_tag.xlsx"  # 需要写入的文件名
lock = threading.Lock()

def write_file(data):
    with lock:
        with open()
        # 将数据写入到文件中
        
    
def read_file():
    with open(file, "r") as f:
        
